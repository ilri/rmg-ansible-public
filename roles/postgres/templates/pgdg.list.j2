# PGDG apt repo lists
{% if ansible_distribution_version == '16.04' %}
# There are no builds for Ubuntu 16.04 (xenial) yet, so use 15.10 (wily)
deb http://apt.postgresql.org/pub/repos/apt/ wily-pgdg main
{% else %}
deb http://apt.postgresql.org/pub/repos/apt/ {{ ansible_distribution_release }}-pgdg main
{% endif %}

#
# LDAP Defaults
#

# See ldap.conf(5) for details
# This file should be world readable but not world writable.

#BASE	dc=example,dc=com
#URI	ldap://ldap.example.com ldap://ldap-master.example.com:666

#SIZELIMIT	12
#TIMELIMIT	15
#DEREF		never

# Ubuntu 14.04 doesn't seem to like cacertdir, so use cacert directly
#TLS_CACERTDIR {{ ldap_conf_dir}}/cacerts
TLS_CACERT {{ ldap_conf_dir}}/cacerts/hpc-ca.asc
TLS_REQCERT     demand

URI ldaps://hpc.ilri.cgiar.org
BASE dc=ilri,dc=cgiar,dc=org

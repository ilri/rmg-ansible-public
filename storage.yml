---
# file: storage.yml

- hosts: storage
  become: yes
  remote_user: provisioning
  vars_files:
    - private/vars/common.yml
    - private/vars/ipsets.yml
  roles:
    - common
    - sssd
    - munin
    - { role: ganglia, when: ansible_distribution == 'CentOS', tags: glusterfs }
    - { role: storage, when: ansible_distribution == 'CentOS', tags: storage }

# vim: set sw=2 ts=2:

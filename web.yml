---
# file: web.yml

- hosts: web
  become: yes
  remote_user: provisioning
  vars_files:
    - private/vars/common.yml
    - private/vars/ipsets.yml
  roles:
    - common
    - munin

# vim: set sw=2 ts=2:
